/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        // removeConsole: true,
        // removeConsole: false,
    },
    compress: true
}

module.exports = nextConfig
